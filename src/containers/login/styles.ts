import {Platform, StyleSheet, TextStyle, ViewStyle} from 'react-native';
import {ITheme} from '../../components';

export interface ILoginStyles {}

export default (theme: ITheme): ILoginStyles => StyleSheet.create({});
