import {
  ImageStyle,
  Platform,
  StyleSheet,
  TextStyle,
  ViewStyle,
} from 'react-native';
import ITheme from '../../components/theme/types';

export interface ISettingsStyles {}

export default (theme: ITheme): ISettingsStyles => StyleSheet.create({});
